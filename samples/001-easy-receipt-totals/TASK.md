# 001 — Receipt totals

`src/receipt.py` does the arithmetic for a shop till: line totals, subtotal,
discount, tax, and the final rounded grand total.

The till has been giving wrong change. `pytest` is red.

**Your task:** make the test suite pass by fixing `src/receipt.py`.

The docstrings in that file describe the intended behaviour, and the tests are
the specification. Read the failures first — not every function is wrong.

See `AGENTS.md` at the repo root for the rules. In short: fix `src/`, never
`tests/`, standard library only.
